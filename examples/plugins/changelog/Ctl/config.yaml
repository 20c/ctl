ctl:
  plugins:

    - type: changelog
      name: changelog
      config:
        # you can paths for the files here
        # these are also exposed to the CLI
        # data_file: CHANGELOG.yaml
        # md_file: CHANGELOG.md

        # config only - there is no --fragments-dir command line argument
        # fragments_dir: changelog.d


  permissions:
    - namespace: ctl
      permission: crud
